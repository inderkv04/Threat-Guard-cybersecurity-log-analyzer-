version: '3.8'

services:
  postgres:
    image: postgres:15
    container_name: log_postgres
    restart: always
    environment:
      POSTGRES_USER: inder
      POSTGRES_PASSWORD: password1234
      POSTGRES_DB: log_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: