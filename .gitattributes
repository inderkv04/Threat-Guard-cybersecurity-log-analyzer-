# Normalize line endings to LF (stops "LF will be replaced by CRLF" warnings)
* text=auto eol=lf

# Explicitly treat these as binary (no line ending conversion)
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.pdf binary
